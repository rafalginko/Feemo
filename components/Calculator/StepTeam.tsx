
import React from 'react';

// This component is deprecated and has been replaced by Settings/TeamSettings.tsx
// The file is kept empty to avoid build errors if referenced, though it should be removed from usage.

export const StepTeam: React.FC = () => {
  return null;
};
